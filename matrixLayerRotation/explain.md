This function rotates a matrix layer-by-layer clockwise by r positions. For each concentric layer, it extracts elements in order (top row → right column → bottom row → left column), rotates them by r % length positions using array slicing, then places them back in the same order. This approach handles rectangular matrices by processing from outer layers inward. Finally, it prints the rotated matrix row by row.